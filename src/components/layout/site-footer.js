export function SiteFooter() {
  return (
    <footer className="mt-24 border-t border-black/5 py-10">
      <div className="shell flex flex-col gap-3 text-sm text-[var(--ink-500)] sm:flex-row sm:items-center sm:justify-between">
        <p>Mercado is a scalable storefront foundation for marketplace growth.</p>
        <p>Next.js 16 App Router, clean feature boundaries, ready for real integrations.</p>
      </div>
    </footer>
  );
}
